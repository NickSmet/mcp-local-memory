# ENV file is needed for SSE mode
# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-api-key-here

# User identifier (optional, defaults to "default_user")
USER_ID=your-name

# SQLite database path (optional, defaults to ~/.mcp-local-memory/memory.db)
SQLITE_PATH=~/.mcp-local-memory/memory.db

# OpenAI models (optional)
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
